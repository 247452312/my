<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="indi.uhyils.dao.OrderNodeRouteDao">


    <sql id="columnList">
        a.prev_node_id as prevNodeId,
		a.update_user as updateUser,
		a.remark as remark,
		a.next_node_id as nextNodeId,
		a.id as id,
		a.create_user as createUser,
		a.create_date as createDate,
		a.delete_flag as deleteFlag,
		a.update_date as updateDate
    </sql>
    <update id="deleteByNodeIds">
        update sys_order_node_route set
        update_date = #{updateDate}
        ,update_user = #{updateUser}
        ,delete_flag = 1
        where prev_node_id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <select id="getByPrevOrderNodeId" resultType="indi.uhyils.pojo.model.OrderNodeRouteEntity">
        select
        <include refid="columnList"/>
        from sys_order_node_route a
        where delete_flag = 0
        and prev_node_id = #{prevOrderNodeId}
    </select>
    <select id="getByOrderNodeIds" resultType="indi.uhyils.pojo.model.OrderNodeRouteEntity">
        select
        <include refid="columnList"/>
        from sys_order_node_route a
        where delete_flag = 0
        and prev_node_id in
        <foreach collection="orderNodeIds" item="fieldId" open="(" separator="," close=")">
            #{fieldId}
        </foreach>
    </select>


</mapper>
